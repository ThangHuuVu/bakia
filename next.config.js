/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      // cms assets
      "www.datocms-assets.com",
      // img
      "img.bakia.vn",
    ],
  },
};

module.exports = nextConfig;
